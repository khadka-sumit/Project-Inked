/**
 * Hooks index
 * Export all custom hooks from this directory
 */

export { useGSAP, useScrollTrigger, gsap, ScrollTrigger } from './useGSAP';
export { useWindowSize, useMediaQuery, useMobile, useTablet, useDesktop, useReducedMotion } from './useResponsive';
